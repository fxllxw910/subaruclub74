services:
  - type: web
    name: subaru-club-bot
    env: python
    region: frankfurt
    buildCommand: "pip install --only-binary=all -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.9
